const { recent, upload, remove, setAuth } = require('../libs/browserstack')

const log = true
const id = 'test-custom-id'
const limit = 3

;(async () => {

  // setAuth('<BrowserStack Auth>')

  // await upload({
  //   id,
  //   log,
  //   file: '</local/path/to/app/build>'
  // })

  // await recent({
  //   id,
  //   log,
  //   limit,
  // })

  // await remove({
  //   log,
  //   id: '<BrowserStack-App-ID>',
  // })

})()